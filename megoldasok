ADATBÁZIS: https://www.w3schools.com/sql/trysql.asp?filename=trysql_select_all
2.: AND / OR / NOT és ORDER BY
1. Feladat

a: SELECT SupplierName FROM suppliers WHERE Country = "UK" OR Country = "USA";

b: SELECT SupplierName FROM suppliers WHERE Country = "USA" AND (city = "New Orleans" OR city = "Boston");

c: SELECT SupplierName FROM suppliers WHERE NOT country = "Japan" OR country = "Canada";


2. Feladat

a: SELECT ProductName, Price FROM Products ORDER BY Price ASC, ProductName;

b:SELECT ProductName, Price FROM Products ORDER BY Price DESC, ProductName;

c:SELECT ProductName, Unit FROM Products ORDER BY ProductName ASC, Unit DESC;
--Azért van 2 order by, mivel ha van két ProductName ami ugyan az, akkor az a kettő között lesz csökkenő a Unit.

INSERT ... INTO és a NULL érték
 1. Feladat 
 
 a: INSERT INTO categories (CategoryName, Description) VALUES ("Spices", "Salt, Pepper, Oregano");
 
 b: INSERT INTO categories (CategoryName) VALUES ("Spices");
 
 c:SELECT CategoryName, Description FROM Categories WHERE Description IS NULL;
 
UPDATE és DELETE - NINCS FELADAT, SAJÁT

UPDATE customers SET CustomerName = "segg" WHERE Address = "Obere Str. 57"; --A WHERE UTASÍTÁS MUSZÁLY!!! E-nélkül az oszlopban az összes sor adatát megváltoztatjuk!!!

DELETE FROM customers WHERE Address = "Obere Str. 57"; -- Ez az egész sort kitörli ahol az Address "Obere Str. 57". Az ID így nem lesz pontos.


4. Fejezet: SQL Nyelvről bővebben

DB ELÉRÉS KONZOLRÓL




CREATE, DROP, ALTER
1:


SQL függvények
1: Feladat

a;

b;

c;


2: Feladat

a;

b;

c;

3. Feladat

a;

b;

c;

4. Feladat

a;

b;

c;
